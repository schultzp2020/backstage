---
'@backstage/cli-module-migrate': patch
---

Fixed migration to the `@backstage-community` namespace leaving source imports unchanged on Windows and in package directories containing glob characters.
